---
title: 'Cold-recall check on 424, before anything else.'
order: 1
---
Blank editor, five minutes: write the shrink condition and state why <code>maxFreq</code> is not recomputed inside the loop. If it comes out clean, the trace-then-rewrite method is validated and becomes the default. If not, the clarity did not convert to recall and the loop needs another pass. Either result is worth having — only skipping it wastes day 7.
