---
title: 'Wed 9 Sep — re-pass the two that faded, ~30 minutes.'
order: 2
done: true
---
The week-3 branch fired: two or more broken recall answers switches Wednesday from the heap build to a re-pass. Both are mechanisms rather than rules, which is what the gap took. <b>@Sendable captures</b> — a captured <code>var</code> is held by reference, which is why even a read is rejected. <b>In-flight dedup</b> — the task exists in the window between started and finished, so a cache of finished images is still empty when caller two arrives. Thirty minutes, leaving room for something else. <b>Ran 9 Sep, and both came back with one prompt each.</b> <a href="/open#sendable-stated-backwards">The boxing was restored unprompted after a single framing question</a>; <a href="/open#in-flight-dedup-mechanism-faded">dedup landed the moment the question was re-asked as a timing question</a> rather than a design one. Two for two on the same morning, which makes the finding a general one: <b>decay is not the same as never having learned it, and repair is cheap when there was a mechanism to repair.</b> The room it left went to the heap, two days early.
