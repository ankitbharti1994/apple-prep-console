---
title: 'State <code>@Sendable</code> cold, using the corrected sentence.'
order: 1
---
Attempted on 27 Aug and failed on mechanism — the behaviour was right, the rule credited for it was not. The target sentence has changed: every captured value must itself be Sendable, and a captured <code>var</code> cannot be referenced at all, because <code>var</code>s are boxed and captured by reference. Still does not contain the word "isolation".
