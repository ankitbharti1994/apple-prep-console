---
title: 'State <code>@Sendable</code> cold — fourth session.'
order: 1
---
Attempted on 27 Aug and failed on mechanism; deferred by choice on 28 Aug, so it opens into a fourth session. The target sentence: every captured value must itself be Sendable, and a captured <code>var</code> cannot be referenced at all, because <code>var</code>s are boxed and captured by reference. Still does not contain the word "isolation". It is now the oldest unclosed item on the board.
