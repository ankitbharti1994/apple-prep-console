---
title: 'Build your own heap, then re-attempt Q13 against it.'
order: 6
---
Top K Frequent is currently solved by sorting — the <code>O(m log m)</code> route the follow-up exists to rule out. Build the heap, then redo it. A hash map is the same kind of phase-1 intuition item, untouched so far, and sits directly adjacent to Q13 and Q14.
