---
title: 'Run the two things that were only reasoned about.'
order: 6
---
Case B — the mutation case, `return { count += 1 }` — never had its diagnostic observed, and the cancellation timing snippet was predicted rather than measured. Both are single-file, single-difference runs. On the same day a third confident prediction — <code>utf8.count</code> of 25 for the ZWJ family — came out at 18 when actually run, which is the argument for clearing these two.
