---
title: 'Concurrency still untouched in practice.'
order: 7
---
Task groups, <code>AsyncSequence</code>, and <code>Task.detached</code> in real code rather than on paper. The in-flight-<code>Task</code> pattern for deduplicating concurrent cache fills was raised on day 2 and still has not been built — a natural candidate for the Saturday block.
