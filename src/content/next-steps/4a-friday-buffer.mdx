---
title: 'Fri 11 Sep — a buffer, and worth spending deliberately.'
order: 4
---
Freed when the heap moved forward two days. Three candidates, in the order they are worth: <b>the testing habit's unexercised half</b> — writing and running a distinguishing artifact, which has <a href="/open#distinguishing-input">never once happened unprompted</a> and is now two problems behind; <b>linked lists</b>, the dependency that <a href="/open#coverage-every-problem-inside-topic-1">forced the phase 2 rewrite</a> and the first topic of the breadth track that starts on the 14th; or <b>binary search</b>. The failure mode to avoid is the one an unallocated day always has — it gets spent on whatever is nearest, and the buffer is the only slack left before the checkpoint on the 13th.
