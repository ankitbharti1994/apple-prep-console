# Test fixtures

Place `apple-prep-console.html` here to run `npm run verify`.

The file is the original prep console HTML (178 KB) used by:

- `scripts/verify-traces.ts`
- `scripts/coverage-check.ts`

`ORIGINAL_HTML` overrides the path if you prefer to keep the file elsewhere.
